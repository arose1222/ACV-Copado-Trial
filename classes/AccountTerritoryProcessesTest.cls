@isTest
// Get rid of one day
public class AccountTerritoryProcessesTest {
    /*@TestSetup
    static void makeData(){
        Account a = new Account(Name='seller',IST_Account_Owner__c =UserInfo.getUserId(),Sales_Territory__C = 'a0m0a00000LKjLUAA1');
        insert a;
    }

    @isTest static void checkSalesRegion(){
        //System.assertEquals('Ohio Valley Region', [SELECT Sales_Region__c FROM Account].Sales_Region__c);
    }

    @isTest static void checkUpdate() {
        Account a = [SELECT Sales_Territory__c FROM Account];
        a.Sales_Territory__c = 'a0m0a00000MWF8wAAH';
        update a;
        //System.assertEquals('Mountain West Region', [SELECT Sales_Region__c FROM Account].Sales_Region__c);
    }*/
}