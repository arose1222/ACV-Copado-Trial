<aura:component implements="force:lightningQuickAction,force:hasRecordId">
    <aura:html tag="style">
        .slds-modal__container {
             width : 60% !important;
             max-width : 60% !important;
         }
    </aura:html>
    <c:addAddressToAccount recordId="{!v.recordId}" onclose="{!c.closeQA}"></c:addAddressToAccount>
</aura:component>